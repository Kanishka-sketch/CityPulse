const steps=["City data","Clean & combine","Spot unusual changes","Find relationships","Why Engine","City health score"];
function HowItWorks(){return <section className="panel pipeline-panel" aria-labelledby="pipeline-title"><div className="panel-heading"><div><span className="eyebrow">A SIMPLE PICTURE OF CITY CONDITIONS</span><h2 id="pipeline-title">How CityPulse works</h2><p className="pipeline-subtitle">From scattered city data to one simple civic picture.</p></div></div><ol className="pipeline">{steps.map((step,index)=><li key={step}><span className="pipeline-icon" aria-hidden="true">{["◉","⌁","⌕","↔","◎","◌"][index]}</span><span>{step}</span>{index<steps.length-1&&<span className="pipeline-arrow" aria-hidden="true">→</span>}</li>)}</ol></section>}
export default HowItWorks;
